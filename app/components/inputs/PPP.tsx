import { FC } from "react"
import PPPSVG from "./SVG/PPP.svg"

type PPPProps = {}

const PPP: FC<PPPProps> = ({}) => {
  return <PPPSVG width={32} height={32} className="white" />
}

export default PPP
